import * as React from "react";

const NotFoundPage = () => (
  <>
    <h1 style={{ textAlign: "center" }}>404 – NOT FOUND</h1>
    <p style={{ textAlign: "center" }}>That route doesn't exist...</p>
  </>
);

export default NotFoundPage;
